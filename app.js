function criptografar() {
    const textArea = document.querySelector("textarea").value
    return console.log(textArea)

    // const shadowArea = document.getElementsByClassName("remove-area")
    // shadowArea.classList.add('')
    // console.log(shadowArea)
}